---
title: "quarto_chunks"
author: "Vasiliki"
format: html
toc: true
toc-ocation: left
grid:
 sidebar-width: 200px
 body-width: 900px
 margin-width: 400px
 gutter-width: 1.5rem
  html: 
  self-contained: true
   theme: 
    light: minty
    dark: superhero
    lang: it
    bibliography: biblio.bib
editor: visual
---

```{r}
head(cars)
```

```{r}
head(cars)
```

```{r}
#| echo: false  

head(cars)
```

```{r}
#| label: fig-distribution
#| layout-ncol: 3
#| fig-cap: Standardized Normal Distribution
#| fig-subcap: 
#|   - "Density"
#|   - "Cumulative"
#|   - "Quantile"
x = seq(-3,3, length.out= 1000)
d = data.frame(x = x, 
               dx = dnorm(x), 
               px = pnorm(x), 
               qx = qnorm(pnorm(x)))
plot(d$x, d$dx, type = "l", lwd = 2, cex = 2, xlab = "x", ylab = "dx")
plot(d$x, d$px, type = "l", lwd = 2, cex = 2, xlab = "x", ylab = "px")
plot(d$px, d$qx, type = "l", lwd = 2, cex = 2, xlab = "px", ylab = "qx")
```
@fig-normalProperties illustrates
properties. @fig-normalProperties-1,
@fig-normalProperties